# update PATH
export PATH="$HOME/scripts/shared:$PATH"
export PATH="$HOME/scripts/{{ .chezmoi.username }}:$PATH"

# Remove duplicates from the PATH 
export PATH=$(echo $PATH | tr ':' '\n' | awk '!seen[$0]++' | tr '\n' ':')
